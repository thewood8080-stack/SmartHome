# כתובת ה-API בפרודקשן.
# הערך האמיתי מוזרק בזמן ה-build בשירות האירוח (משתנה סביבה VITE_API_URL),
# ולכן הוא נשאר ריק כאן ולא מקובע בקוד.
# חשוב: origin בלבד, בלי /api ובלי סלאש בסוף — הקוד מוסיף את שניהם.
VITE_API_URL=
