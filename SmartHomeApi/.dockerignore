# תוצרי בנייה מקומיים — נבנים מחדש בתוך התמונה
bin/
obj/

# בסיס הנתונים המקומי. אסור שייכנס לתמונה: הוא יחזיר נתוני פיתוח לשרת
# ויסתיר את בסיס הנתונים שנוצר שם.
smarthome.db
smarthome.db-shm
smarthome.db-wal
*.db
*.db-shm
*.db-wal

# סודות מקומיים — סיסמת המנהל הראשוני יושבת כאן, ואסור שתיצרב לתוך התמונה.
appsettings.Local.json

# סביבת פיתוח
Properties/launchSettings.json
.vs/
